[pytest]
addopts =
;    --numprocesses auto
;    --cov tom_calculator --cov-report term-missing
    --verbose
    --color=yes
    --show-capture=no

markers =
    unit: Unit tests
    integration: Integration tests
